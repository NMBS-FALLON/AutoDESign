<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SalesBot">

    <Style TargetType="Button">
        <Setter Property="FontFamily" Value="Corbel" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Margin" Value="10" />
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <ControlTemplate x:Key="HorzontalSeperator" TargetType="Separator">
        <Rectangle HorizontalAlignment="Stretch" Height="1" Margin="0" Stroke="Black" />
    </ControlTemplate>

    <ControlTemplate x:Key="VerticalSeperator" TargetType="Separator">
        <Rectangle VerticalAlignment="Stretch" Width="1" Margin="0" Stroke="Black" />
    </ControlTemplate>




</ResourceDictionary>